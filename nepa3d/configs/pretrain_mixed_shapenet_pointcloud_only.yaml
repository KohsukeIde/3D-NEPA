# ShapeNet-only pointcloud pretraining corpus (fair baseline).
# No ScanObjectNN component is included.

mix_seed: 0
mix_num_samples: 200000
replacement: true

datasets:
  - name: shapenet_pc
    cache_root: data/shapenet_cache_v0
    split: train
    backend: pointcloud_noray
    weight: 1.0
