# PatchNEPA Code

PatchNEPA is split by active sub-line:

- `mainline/`: patch-token PatchNEPA base model and direct finetune path
- `tokens/`: active v2 token-stream pretraining branch
- `cqa/`: additive explicit-query primitive answering branch

Use these directories directly when working on a specific PatchNEPA line.
