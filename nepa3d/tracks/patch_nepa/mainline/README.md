# PatchNEPA Mainline

This directory contains the base PatchNEPA implementation:

- `models/`: PatchNEPA backbone plus direct classification backbones
- `train/`: PatchNEPA pretrain and direct finetune entrypoints
- `configs/`: legacy/mainline PatchNEPA configs

The active v2 token-stream branch is tracked separately under `../tokens/`.
