# QueryNEPA Code

This track contains the legacy token-level QueryNEPA implementation.

- `models/`: QueryNEPA backbone and heads
- `train/`: legacy pretrain / finetune entrypoints
- `configs/`: QueryNEPA-era YAML configs

New feature work should target PatchNEPA first. Keep this track for
reproducibility and historical comparison.
