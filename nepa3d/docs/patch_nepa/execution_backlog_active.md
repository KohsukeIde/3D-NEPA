# PatchNEPA Execution Backlog

Last updated: 2026-03-27

## 1. Purpose

This file is the canonical operational backlog for the active PatchNEPA line.

Use this page when asking:

- what is still unresolved,
- what should run next on the local GPU machine,
- what the success/fail rule is for each queued experiment,
- where the canonical output must be recorded after completion.

This file is the source of truth for execution order and gating.

## 2. Operational Contract

- primary execution surfaces:
  - local Linux GPU machine for cheap analysis / short local checks
  - ABCI for structured cluster runs such as worldvis rebuilds and CQA smokes
- PBS/QF launchers are not the operational default during this phase
- canonical scientific conclusions still live in:
  - `storyline_query_to_patch_v2_active.md`
  - `hypothesis_matrix_active.md`
  - `restart_plan_patchnepa_data_v2_20260303.md`
  - `runlog_patch_nepa_202602.md`
- queue/runtime state lives in:
  - `scripts/local/patchnepa_local_queue.tsv`
  - `scripts/local/patchnepa_local_queue_runner.sh`
  - `scripts/local/patchnepa_local_status.sh`
- runtime completion is **not** canonical completion
- a row is only canonized after the result is written back into
  `restart_plan_patchnepa_data_v2_20260303.md` or
  `runlog_patch_nepa_202602.md`, and then summarized in
  `storyline_query_to_patch_v2_active.md` if it changes the current read

## 3. Status and Cost

Status values:

- `queued`
- `running`
- `done-unbackfilled`
- `canonized`
- `blocked`
- `deferred`
- `manual`

Local budget classes:

- `A`: no-GPU analysis / log aggregation / W&B summary extraction / plots
- `B`: 1-GPU short smoke or mini-eval, target `<= 2h`
- `C`: 1-GPU overnight, target `<= 12h`
- `D`: multi-day or multi-GPU local run

Policy:

- default queue target is `A-C`
- `D` requires a successful lower-cost gate first
- full300-class local reruns are banned unless they directly decide the paper

## 4. Canonicalization Workflow

- source priority:
  - result JSON
  - W&B summary
  - structured log root
- do not use top-level stdout/stderr as the only retained result
- queue state progression:
  - `queued -> running -> done-unbackfilled -> canonized`
- if a row fails:
  - keep the failed runtime record in `logs/local_queue/.../state.tsv`
  - do not promote the result into docs until the failure mode itself is worth
    recording

## 5. Backlog Table

| ID | Priority | Question | Class | Status | Local Cost | Gating | Canonical Input | Canonical Output | Decision Rule | Canonize Target | Notes |
|---|---:|---|---|---|---|---|---|---|---|---|---|
| `L000A` | `5` | Can a visibility-first world-package rebuild (`mesh_qry_vis_sig`, `mesh_qry_ao`, `pc_ctx_bank_xyz`, `udf_surf_probe_*`) complete cleanly and keep short-run reconstruction healthy? | `D` | `queued` | `multi-stage local rebuild + 2 GPU short screen` | none | `data/ShapeNetCore.v2` + `scripts/local/patchnepa_visocc_branch.sh` + `nepa3d/configs/shapenet_unpaired_mix_v2_tokens_drop1_pc33_mesh33_udf33_visocc{,_base}.yaml` | `logs/local_patchnepa_visocc/patchnepa_visocc_l000ab_20260311/decision.json` | `visocc` stays alive only if short pretrain keeps `recon_lift_q >= 0` and `recon_lift_a > 0`; otherwise close the branch as an exploratory negative result. | `restart_plan -> runlog -> hypothesis_matrix if branch starts` | This row now owns the schema-breaking world-package rebuild plus the baseline/visocc short pretrain screen. |
| `L000B` | `6` | Under one fixed mini-CPAC protocol, does the world-package `visocc` line beat the same-lineage baseline enough to justify a follow-up `answer_only` arm? | `B` | `queued` | `<=2h once L000A ckpts exist` | `L000A` short pretrain ckpts | `results/local_patchnepa_cpac/l000b_visocc_{base,pc33,answeronly}.json` + `logs/local_patchnepa_visocc/patchnepa_visocc_l000ab_20260311/decision.json` | Promote only if `visocc` improves mini-CPAC by either `iou@0.01 >= baseline + 0.005` or `rmse <= baseline - 0.002` without catastrophic regression (`iou@0.01 < baseline - 0.003` or `rmse > baseline + 0.003`). | `restart_plan -> runlog -> storyline only if the branch materially changes the read` | Executed by the same local visibility pipeline after the short pretrain arms complete. |
| `D001` | `7` | Can the existing `worldvis` source cache be frozen as `world_v3` without a full rebuild, and how large is the strict clean subset? | `A` | `canonized` | `1 CPU-node freeze sprint` | none | `data/shapenet_cache_v2_20260311_worldvis` + `scripts/preprocess/submit_world_v3_freeze_qc.sh` | `results/data_freeze/world_v3_freeze_20260316_qc/{augment_world_v3_summary,world_v3_audit_summary,subset_watertight_summary}.json` | Freeze succeeds if the existing cache can be augmented in place with zero errors and yields a canonical audit + subset manifest; only escalate to full rebuild if contract-level defects remain. | `restart_plan -> runlog -> storyline` | CPU job `113209` completed. `world_v3` augmentation updated `39761` files, skipped `12550`, and saw `0` errors. Audit shows `visibility_fallback_used_rate=0.0`, `watertight_rate=0.24%`, and strict `subset_watertight` keeps only `119/52311`, so the base corpus remains the mainline source cache and the clean subset is pivot-only. |
| `C001` | `8` | Are the current CQA targets healthy enough to justify longer training, or are they dominated by majority-code collapse? | `A` | `canonized` | `<=2h audit` | none | `data/shapenet_unpaired_cache_v2_20260311_worldvis_drop1` + `nepa3d/analysis/audit_cqa_targets.py` | `results/cqa_audit/patchnepa_cqa_target_audit_20260315/summary.json` | Demote any task with `top1 code share > 0.8` or near-zero per-shape entropy; use the audit to decide which tasks survive the next smoke. | `restart_plan -> runlog -> storyline if the CQA read changes` | Current audit keeps `udf_distance` as the healthiest task; `udf_clearance` remains front-only in current code; `mesh_visibility` and `udf_thickness` are not headline-safe smoke tasks. |
| `C002` | `9` | With the same explicit-query branch, does `surf context -> udf_distance + udf_clearance(front)` beat the current majority baseline and show real prompt use? | `B` | `canonized` | `2k-step ABCI smoke` | `C001` | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfsurf.yaml` + `scripts/abci/submit_patchnepa_current_cqa_smoke.sh` | `runs/cqa/patchnepa_cqa_udfsurf_20260315/cqa_udfdist_clearfront_g2_s2000/eval_controls_128.json` + W&B CQA smoke run | Keep as mainline candidate only if majority collapse weakens (`top1 share < 0.6`) and controls move materially (`no_context delta_ce >= +0.3` or task-wise token acc drops by `>= 3pt`). | `restart_plan -> runlog -> storyline if CQA read changes` | ABCI job `113068` completed. `udf_distance` beat the same-slice majority baseline (`0.1093` vs `0.0586`) and is query-sensitive (`shuffled_query delta_ce=+2.039`, `acc=-7.42pt`), but context sensitivity is still weak (`no_context delta_ce=+0.206`, `acc=-2.66pt`). `udf_clearance` remained majority-like (`acc=0.7012` vs baseline `0.7015`). |
| `C003` | `10` | If the same UDF tasks are prompted from `pc_bank`, does heterogeneous context increase prompt sensitivity? | `B` | `canonized` | `2k-step ABCI smoke` | `C001` | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfpcdiag.yaml` + `scripts/abci/submit_patchnepa_current_cqa_smoke.sh` | `runs/cqa/patchnepa_cqa_udfpcdiag_20260315/cqa_udfdist_clearfront_pcdiag_g2_s2000/eval_controls_128.json` + W&B CQA smoke run | Diagnostic only; do not use as unpaired headline evidence. Success means `pc_bank` raises context sensitivity relative to `surf` on the same tasks. | `restart_plan -> runlog only; storyline only if it changes the branch read` | ABCI job `113071` completed. `pc_bank` did not materially strengthen context use relative to `surf`: `udf_distance` stayed above majority (`0.1110` vs `0.0586`) and query-sensitive (`shuffled_query delta_ce=+1.914`, `acc=-7.74pt`), but `no_context delta_ce` weakened to `+0.140` and `shuffled_context` stayed near-zero (`+0.004`). `udf_clearance(front)` remained majority-like (`0.7225` vs baseline `0.7015`). |
| `C004` | `11` | Is `udf_thickness` still worth keeping after the audit, or should it stay demoted behind `udf_distance/clearance`? | `B` | `blocked` | `2k-step ABCI smoke if reopened` | reopen only if `C001` and `C002` show sane entropy or improved control deltas | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa.yaml` or a thinner replacement config | `runs/cqa/.../eval_controls_128.json` + audit JSON | Reopen only if the first-bin / top-1 code share is materially lower than the current `~0.66` read, or if a new codec changes the target entropy. | `restart_plan -> runlog if reopened` | Current `udf_thickness` is conditional because the first-bin dominates the present smoke. |
| `C005` | `12` | Does a single-task `udf_distance` smoke move the branch beyond mixed-task query routing and show clearer shape-conditioned use? | `B` | `canonized` | `2k-step ABCI smoke` | `C001-C003` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist.yaml` + `scripts/abci/submit_patchnepa_current_cqa_smoke.sh` | `runs/cqa/patchnepa_cqa_udfdist_20260315/cqa_udfdist_g2_s2000/eval_controls_128.json` + W&B CQA smoke run | Keep only if it stays above its same-slice majority baseline and `wrong_shape`/`no_context` degrade more clearly than the mixed-task read. | `restart_plan -> runlog -> storyline if branch read changes` | ABCI job `113102` completed. `udf_distance` stayed well above the majority baseline (`0.1552` vs `0.0586`) with a broader prediction spread (`pred_top1_share=0.1093`, `31` predicted codes). `no_context` now moves strongly (`delta_ce=+0.6238`, `acc=-5.46pt`), but `wrong_shape` remains modest (`delta_ce=+0.1100`, `acc=-1.59pt`), so the branch is more clearly context-using but still not strongly shape-conditioned. |
| `C006` | `13` | If `udf_distance` still looks query-dominant, does a `near_surface` hard-query subset force stronger shape-conditioned use? | `B` | `canonized` | `2k-step ABCI smoke` | `C005` first | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist_near.yaml` + `scripts/abci/submit_patchnepa_current_cqa_smoke.sh` | `runs/cqa/patchnepa_cqa_udfdist_near_20260315/cqa_udfdist_near_g2_s2000/eval_controls_128.json` + W&B CQA smoke run | Promote only if `wrong_shape` and `no_context` degrade materially more than the full `udf_distance` run without collapsing back to a majority code. | `restart_plan -> runlog -> storyline if it changes the CQA read` | ABCI job `113125` completed. The naive `near_surface` subset collapsed: `udf_distance` fell below the same-slice majority baseline (`0.0417` vs `0.0439`), prediction spread shrank to `6` codes with `pred_top1_share=0.3959`, and all controls except `wrong_type` became inert (`|delta_ce| < 0.001`). Hard-query via `query_src_filter=near_surface` is a negative result under the current task/codec recipe. |
| `C007` | `14` | After the hard-query pass, does `pc_bank` help once `udf_distance` is restricted to `near_surface` queries? | `B` | `canonized` | `2k-step ABCI smoke` | `C006` first | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist_near_pcdiag.yaml` + `scripts/abci/submit_patchnepa_current_cqa_smoke.sh` | `runs/cqa/patchnepa_cqa_udfdist_near_pcdiag_20260315/cqa_udfdist_near_pcdiag_g2_s2000/eval_controls_128.json` + W&B CQA smoke run | Diagnostic only; success means `wrong_shape`/`no_context` sensitivity improves over the `surf` hard-query run rather than merely matching query-only behavior. | `restart_plan -> runlog -> storyline only if it changes the branch read` | ABCI job `113127` completed. `pc_bank` did not rescue the `near_surface` collapse: `udf_distance` stayed below majority (`0.0433` vs `0.0441`), `pred_top1_share` increased to `0.4690`, and `no_context`, `wrong_shape`, `shuffled_context`, and `shuffled_query` all stayed effectively zero-change. Do not treat `pc_bank` as helpful evidence under the current hard-query setup. |
| `C008` | `15` | On the frozen `world_v3` contract, does the original full-range single-task `udf_distance` positive signal reproduce canonically? | `B` | `canonized` | `2k-step ABCI rerun` | `D001` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist.yaml` + `scripts/abci/submit_patchnepa_current_cqa_udfdist_reval.sh` | `runs/cqa/patchnepa_cqa_udfdist_worldv3_reval_20260316/cqa_udfdist_worldv3_g2_s2000/eval_controls_128.json` + W&B reval run | Revalidate only if the frozen-cache rerun stays above majority and preserves meaningful `no_context` sensitivity; the new control focus is `wrong_shape_same_synset` and `wrong_shape_other_synset` under deterministic-random eval sampling. | `restart_plan -> runlog -> storyline if the CQA read changes` | ABCI job `113212` completed. The frozen-cache rerun reproduced and strengthened the positive signal: `acc=0.1064` vs majority `0.0376`, `pred_top1_share=0.2118`, `pred_unique_codes=33`, `no_context delta_ce=+1.0599`, `wrong_shape_same delta_ce=+0.3193`, `wrong_shape_other delta_ce=+1.1527`, `shuffled_query delta_ce=+2.7418`. `uniform` queries are already strong (`acc=0.1739`), while `near_surface` remains much weaker (`0.0409`). |
| `C009` | `16` | If `C008` reproduces, does a 10k-step `udf_distance` learning curve grow from context-conditioned use into stronger shape-conditioned use? | `C` | `canonized` | `10k-step ABCI run + checkpoint-curve eval` | `C008` must finish first | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist.yaml` + `scripts/abci/submit_patchnepa_current_cqa_udfdist_curve.sh` | `runs/cqa/patchnepa_cqa_udfdist_worldv3_curve_20260316/cqa_udfdist_worldv3_g2_s10000/eval_curve_128/curve_summary.json` + final `eval_controls_128.json` | Continue the branch only if the learning curve keeps widening the gap above majority **and** `wrong_shape_same/other_synset` deltas rise materially over time; if only `no_context` improves, keep CQA as context-conditioned but not shape-conditioned. | `restart_plan -> runlog -> storyline after completion` | ABCI job `113213` completed. The 10k curve is clearly positive: final `acc=0.3173` vs majority `0.0376`, `pred_top1_share=0.1339`, `pred_unique_codes=40`, `no_context delta_ce=+6.5064`, `wrong_shape_same delta_ce=+4.1433`, `wrong_shape_other delta_ce=+10.2148`, `shuffled_query delta_ce=+11.1499`. The curve grows steadily from `500 -> 10000` steps, with shape-conditioned controls becoming large rather than staying near zero. |
| `C010` | `17` | Does the frozen-`world_v3` `surf -> udf_distance` checkpoint transfer zero-shot to `pc_bank -> udf_distance` when only eval-time context is changed? | `B` | `canonized` | `eval-only ABCI off-diagonal test` | `C009` canonized | `runs/cqa/patchnepa_cqa_udfdist_worldv3_curve_20260316/cqa_udfdist_worldv3_g2_s10000/ckpt_final.pt` + `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist_pcbank.yaml` + `scripts/abci/submit_patchnepa_current_cqa_udfdist_offdiag.sh` | `results/cqa_eval/patchnepa_cqa_udfdist_offdiag_20260316_171008/cqa_udfdist_offdiag_eval.json` | Off-diagonal transfer is worth keeping only if `pc_bank` stays materially above majority and preserves positive `no_context` / `wrong_shape_other_synset` deltas on the held-out `eval` split. | `restart_plan -> runlog -> storyline if off-diagonal survives` | ABCI job `113431` completed. Zero-shot `pc_bank -> udf_distance` is clearly positive: `acc=0.1766` vs majority `0.0397`, `pred_top1_share=0.0757`, `pred_unique_codes=39`, `no_context delta_ce=+4.4730`, `wrong_shape_same delta_ce=+1.8570`, `wrong_shape_other delta_ce=+4.6121`, `shuffled_query delta_ce=+5.8515`. `uniform` remains much stronger than `near_surface` (`0.3169` vs `0.0338`), but the off-diagonal transfer itself is real. |
| `C011` | `18` | Can the same `udf_distance` checkpoint produce method-native dense-grid completion metrics beyond token accuracy on frozen `world_v3`? | `B` | `canonized` | `ABCI dense-grid completion pilot` | `C009` canonized | `runs/cqa/patchnepa_cqa_udfdist_worldv3_curve_20260316/cqa_udfdist_worldv3_g2_s10000/ckpt_final.pt` + `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist.yaml` + `scripts/abci/submit_patchnepa_current_cqa_udfdist_completion.sh` | `results/cqa_completion/patchnepa_cqa_udfdist_completion_20260316_171008/cqa_udfdist_completion_grid12.json` | Keep as method-native completion evidence only if dense-grid `MAE/RMSE/IoU@τ` are stable on held-out shapes and the decode does not collapse back to trivial constant grids. | `restart_plan -> runlog -> storyline if completion is meaningful` | ABCI job `113432` completed. The dense-grid pilot is positive on `16` held-out shapes at `grid_res=12`: `MAE=0.0479±0.0103`, `RMSE=0.0638±0.0132`, `IoU@0.01=0.0814`, `IoU@0.02=0.1850`, `IoU@0.05=0.5712`. This is sufficient to treat `udf_distance` as method-native answering/completion, not just token-level probing. |
| `C012` | `19` | Do the positive `udf_distance` mainline and zero-shot off-diagonal reads reproduce across seeds, rather than only at seed `0`? | `B` | `canonized` | `2x 10k ABCI runs + 2x dependent eval-only off-diagonal` | `C009-C010` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist.yaml` + `scripts/abci/submit_patchnepa_current_cqa_udfdist_seedpack.sh` | `runs/cqa/<seeded_run_set>/.../eval_controls_128.json` + `results/cqa_eval/<seeded_run_set>/*.json` | Promote `udf_distance` to a stable paper-method branch only if seeds `1/2` preserve the same qualitative read: mainline above majority with positive `wrong_shape_same/other` and off-diagonal `pc_bank` transfer that remains meaningfully above majority. | `restart_plan -> runlog -> storyline if the seed pack confirms or overturns the read` | ABCI jobs `113438-113441` completed. Seeds `1/2` confirm the seed-`0` read. Mainline `udf_distance` stays far above majority (`0.3330/0.3446` vs `0.0385/0.0383`) with strong controls (`delta_ce(no_context)=+8.35/+6.25`, `wrong_shape_same=+4.59/+4.29`, `wrong_shape_other=+10.97/+10.77`). Zero-shot `pc_bank` off-diagonal also reproduces (`0.1584/0.1620` vs `0.0402/0.0379`) with positive `no_context` and `wrong_shape_same/other` deltas. `udf_distance` is now a stable paper-method branch rather than a single-seed read. |
| `C013` | `20` | If zero-shot off-diagonal remains limited, what is the paired `pc_bank -> udf_distance` upper bound under the same model/training recipe? | `B` | `canonized` | `10k ABCI upper-bound train + eval` | only open after `C012` lands | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist_pcbank.yaml` + `scripts/abci/submit_patchnepa_current_cqa_udfdist_pcbank_upper.sh` | `runs/cqa/<run_set>/.../eval_controls_128.json` | Use only as a diagnostic upper bound, not as mainline evidence. Open it only if `C012` leaves ambiguity about whether zero-shot transfer is representation-limited or task-limited. | `restart_plan -> runlog only if executed` | ABCI job `113472` completed. Paired `pc_bank -> udf_distance` is clearly positive and improves over zero-shot (`acc=0.2533` vs zero-shot `0.1766`, majority `0.0375`), with strong controls (`delta_ce(no_context)=+5.5981`, `wrong_shape_same=+1.8220`, `wrong_shape_other=+4.5715`, `shuffled_query=+7.8943`). Treat it as a diagnostic upper bound: it shows extra headroom from paired cross-primitive training, but the cleaner main claim remains the already-positive zero-shot off-diagonal transfer. |
| `C014` | `21` | Does the `udf_distance` CQA encoder retain paper-safe recognitional utility on canonical ScanObjectNN when evaluated as a downstream utility check rather than a headline task? | `C` | `canonized` | `1x QG finetune per variant` | `C012` canonized | `runs/cqa/patchnepa_cqa_udfdist_worldv3_curve_20260316/cqa_udfdist_worldv3_g2_s10000/ckpt_final.pt` + `scripts/eval/nepa3d_cqa_cls_qg.sh` + canonical ScanObjectNN cache | `runs/cqa_cls/<run_tag>/ckpt_best.pt` + `logs/cqa_cls/<run_tag>.log` | Treat as utility evidence, not a SOTA chase. Success means the CQA encoder is clearly above chance/scratch and not catastrophically below the current `recong2` utility line; start with `obj_only`, then extend to `obj_bg/pb_t50_rs` only if the first run is sane. | `restart_plan -> runlog -> storyline only if utility meaningfully changes the paper read` | `pointmae(test-as-val)` utility is now fixed: `obj_only=0.8468`, `obj_bg=0.8399`, `pb_t50_rs=0.7765`, `obj_only/bos=0.8451`. This is paper-safe utility evidence but not a new headline line. |
| `C015` | `22` | Can `udf_distance` completion be canonicalized at mesh level for both same-context and zero-shot off-diagonal settings, rather than remaining a small dense-grid pilot? | `C` | `canonized` | `1-2x QG translation/completion runs` | `C010-C013` canonized | `runs/cqa/patchnepa_cqa_udfdist_worldv3_curve_20260316/cqa_udfdist_worldv3_g2_s10000/ckpt_final.pt` + `scripts/analysis/nepa3d_cqa_udfdist_translation_qg.sh` + same/offdiag mix configs | `results/cqa_completion/<run_tag>.json` + qualitative figure pack under `results/cqa_completion/` | Promote completion to headline evidence only if `64-128` held-out shapes at `grid_res>=16` keep stable `MAE/RMSE/IoU@tau` **and** yield mesh-side `Chamfer/F-score` plus a publishable qualitative grid (`GT / same / offdiag / no_context`). | `restart_plan -> runlog -> storyline after canonical completion lands` | Canonical mesh-side pack landed at `64` shapes / `grid_res=16` with assets export. Same-context: `MAE=0.0519`, `RMSE=0.0675`, `IoU@0.05=0.5517`, `mesh_chamfer_l2=0.01257`, `mesh_fscore=0.03870`. Off-diagonal: `MAE=0.1242`, `RMSE=0.1796`, `IoU@0.05=0.3523`, `mesh_chamfer_l2=0.05360`, `mesh_fscore=0.02697`. |
| `C016` | `23` | Is there at least one external frozen point-only baseline that can be evaluated on the same `pc_bank -> udf_distance` harness, so the task is not only an internal self-comparison? | `A` | `deferred` | `implementation plan + one baseline adapter` | open after `C015` is stable | `nepa3d/docs/patch_nepa/cqa_external_baseline_plan.md` + future external checkpoint adapter | `results/cqa_eval/<external_baseline>.json` + `results/cqa_completion/<external_baseline>.json` | Open only after the CQA same/offdiag completion path is canonical. The first target is a frozen Point-MAE encoder with a lightweight readout on the same token/control/completion harness. | `restart_plan -> runlog -> storyline if external context changes the read` | This stays deferred because the live repo still lacks the checkpoint-loading glue; the plan doc records the intended adapter surface. |
| `C017` | `24` | Under an explicit ordered spatial query list, does AR regain an advantage over the current shuffled-query read on `udf_distance`? | `C` | `canonized` | `10k ABCI ordered-AR train + offdiag eval` | `C012-C015` canonized | `scripts/abci/submit_patchnepa_current_cqa_udfdist_curve.sh` with `QUERY_ORDER=ordered_xyz` + ordered offdiag eval | `runs/cqa/<run_set>/.../eval_controls_128.json` + `results/cqa_eval/<run_set>/...json` | Success means ordered AR materially improves over shuffled AR and narrows or reverses the current `parallel > AR` read; otherwise the current conclusion stays “AR is not needed under shuffled queries, and may not be needed at all.” | `restart_plan -> runlog -> storyline once ordered-vs-shuffled is read` | Ordered-AR does **not** overturn the existing read. Same-context token acc rises only slightly (`0.3278` vs shuffled `0.3173`), off-diagonal token acc is unchanged (`0.1772` vs `0.1766`), and completion worsens on same-context (`MAE=0.0600`, `IoU@0.05=0.4985`, `mesh_fscore=0.0303` vs shuffled `0.0519/0.5517/0.0387`). Ordered AR strengthens control deltas, but it does not recover a clear advantage over parallel. |
| `C018` | `25` | If query order is itself a design variable, does ordered parallel also improve, or is any gain specific to AR? | `C` | `canonized` | `10k ABCI ordered-parallel train + offdiag eval` | `C012-C015` canonized | `scripts/abci/submit_patchnepa_current_cqa_udfdist_curve_nonar.sh` with `QUERY_ORDER=ordered_xyz` + ordered offdiag eval | `runs/cqa/<run_set>/.../eval_controls_128.json` + `results/cqa_eval/<run_set>/...json` | Compare directly against both `C017` and the existing shuffled parallel row. If ordered parallel improves similarly to ordered AR, the story stays “Q/A schema matters more than AR”; if only AR improves, ordered sequential structure becomes the key factor. | `restart_plan -> runlog -> storyline once ordered-vs-shuffled is read` | Ordered-parallel remains the strongest same-context CQA line among the ordered runs (`acc=0.3645` vs ordered-AR `0.3278`), but ordering does not help it either: shuffled-parallel is still better on same-context completion (`MAE=0.0249`, `IoU@0.05=0.7051`, `mesh_fscore=0.1142` vs ordered `0.0296/0.6516/0.0808`). Off-diagonal ordered-parallel is only slightly above shuffled-parallel on token acc (`0.2168` vs `0.2057`) and slightly below on completion (`IoU@0.05=0.3928` vs `0.4044`). The safest read stays: Q/A schema matters more than AR, and shuffle alone does not explain the earlier AR gap. |
| `C019` | `26` | Is the current `parallel` gain coming from joint answer-slot interaction, or does a strictly independent non-AR answer block still match AR/parallel on the same shuffled protocol? | `C` | `running` | `10k ABCI independent non-AR train + offdiag/completion` | `C012-C018` canonized | `scripts/abci/submit_patchnepa_current_cqa_udfdist_curve_independent.sh` + offdiag/completion wrappers | `runs/cqa/<run_set>/.../eval_controls_128.json` + `results/cqa_eval/<run_set>/...json` + `results/cqa_completion/<run_set>/...json` | If independent stays close to joint non-AR, then answer-to-answer interaction is not doing much and the clean claim becomes “context+query is enough.” If it drops sharply while still beating AR, then the current gain is coming from non-causal joint decoding rather than strictly independent answers. | `restart_plan -> runlog -> storyline once independent-vs-joint is read` | Current run set: `115281-115284` with train `answer_factorization=independent`, train `query_order=shuffled`, and eval/completion on the same sampled protocol as the existing shuffled baselines. |
| `C020` | `27` | Does a shared `DISTANCE + NORMAL` checkpoint already exhibit promptable multi-type behavior under the current strongest factorization (`independent`), before reopening riskier types like `THICKNESS`? | `C` | `canonized` | `10k ABCI shared pretrain + same/offdiag suite` | `C012-C019` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_dist_norm{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_distnorm_shared.sh` + `scripts/abci/submit_patchnepa_current_cqa_multitype_suite.sh` | `runs/cqa/<run_set>/.../ckpt_final.pt` + `results/cqa_multitype/<run_set>/{*.json,*.csv,*.md}` | Promote multi-type promptability only if both `udf_distance` and `mesh_normal` stay above their majority baselines on same-context and remain meaningfully context-sensitive under `pc_bank` off-diagonal evaluation. | `restart_plan -> runlog -> storyline once 2-type shared read is stable` | ABCI jobs `115782-115783` completed. The first `DISTANCE + NORMAL` shared checkpoint stays alive on both tasks: `udf_distance` remains strong (`same acc=0.3765` vs majority `0.0379`, `offdiag acc=0.1959` vs `0.0397`) and `mesh_normal` also stays above majority on same/offdiag (`0.3181/0.2119` vs `0.1602/0.1561`) with positive `no_context` / `wrong_shape_*` deltas, but the `mesh_normal` margin is still modest. Treat this as a passed first multi-type gate, not yet a final framework headline. |
| `C021` | `28` | Can the first multi-type shared checkpoint produce a paper-face TYPE-switch figure from the same frozen model and same context, with only the task type changed? | `C` | `canonized` | `1x QG asset export after C020 train` | `C020` train ckpt | `scripts/abci/submit_patchnepa_current_cqa_type_switch.sh` + `nepa3d/tracks/patch_nepa/cqa/analysis/export_promptable_type_switch_assets.py` | `results/cqa_multitype/<run_set>/index.json` + per-shape `context_points.ply`, `mesh_normal_{pred,gt}_points.ply`, `udf_distance_fields.npz`, `udf_distance_{pred,gt_levelset}_mesh.obj` | Canonize only if the same checkpoint and same context produce semantically distinct `DISTANCE` vs `NORMAL` artifacts that are visually publishable. | `restart_plan -> runlog -> storyline after figure asset pack lands` | ABCI job `115784` completed. The same frozen `DISTANCE + NORMAL` checkpoint now emits task-distinct assets from the same `pc_bank` context (`context_points.ply`, `mesh_normal_{pred,gt}_points.ply`, `udf_distance_{pred,gt_levelset}_mesh.obj`, `udf_distance_fields.npz`). The TYPE-switch path is technically working, but current `mesh_normal` qualitative quality is too weak for a paper-face Figure 1, so keep this as a working asset pack rather than a final publishable figure. |
| `C022` | `29` | If fixed-target answering already removes the old continuation failure modes, does `independent + udf_distance` still need discrete CE, or can a continuous scalar regressor match it under the same shuffled protocol? | `C` | `canonized` | `10k ABCI continuous target-design ablation + offdiag/completion` | `C019` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_udfdist_continuous{,_pcbank}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_udfdist_continuous.sh` | `runs/cqa/<run_set>/.../eval_controls_128.json` + `results/cqa_eval/<run_set>/*.json` + `results/cqa_completion/<run_set>/*.json` | Keep continuous target design alive only if same/offdiag completion stays competitive with the current discrete independent line **and** controls keep strong `no_context` / `wrong_shape_*` sensitivity without low-variance collapse. | `restart_plan -> runlog -> storyline once continuous-vs-discrete is read` | ABCI jobs `115851-115854` completed. Continuous `udf_distance` does **not** collapse: same-context controls give `MAE=0.01225`, `RMSE=0.01670`, `IoU@0.05=0.8793` with large `no_context` / `wrong_shape_*` deltas. Dense completion is strong on same-context (`MAE=0.01319`, `IoU@0.05=0.7575`) and remains viable off-diagonal (`MAE=0.13625`, `IoU@0.05=0.4043`), but off-diagonal and mesh metrics are mixed versus the discrete independent line. Keep continuous target design as a live ablation, not a mainline replacement yet. |
| `C023` | `30` | Once answer-answer interaction is removed, does the full query block still matter, or is `udf_distance` already explained by per-query anchors plus shared context? | `C` | `canonized` | `2x 10k ABCI query-interface ablation + dependent offdiag/completion` | `C019` canonized | `scripts/abci/submit_patchnepa_current_cqa_udfdist_qblockpack.sh` with `query_interface_mode in {self_q,no_q}` | `runs/cqa/<run_set>/.../eval_controls_128.json` + `results/cqa_eval/<run_set>/*.json` + `results/cqa_completion/<run_set>/*.json` | If `no_q ≈ full_q`, the explicit query block is redundant; if `self_q ≈ full_q > no_q`, self-query contextualization is enough; if `full_q > self_q > no_q`, full query-set conditioning is doing real work. | `restart_plan -> runlog -> storyline once full_q/self_q/no_q are read` | ABCI jobs `115858-115865` completed. `full_q` remains the best `independent` row, but `self_q` and `no_q` are close: same token acc `0.3898 / 0.3777 / 0.3772`, offdiag `0.2235 / 0.2286 / 0.2157`, with strong `no_context` / `wrong_shape_*` controls still intact. Completion also drops only slightly from `full_q` to `self_q/no_q`. Read: full query-set conditioning helps a little, but the main mechanism is shared context + per-query anchor rather than the full query list. |
| `C024` | `31` | If continuous target design stays viable for `udf_distance`, does a shared `DISTANCE + NORMAL` continuous checkpoint keep both tasks alive under the same independent promptable interface? | `C` | `canonized` | `10k ABCI shared continuous pretrain + same/offdiag suite` | `C020-C023` stable enough to extend target design to multi-type | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_dist_norm_continuous{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_distnorm_continuous.sh` | `runs/cqa/<run_set>/.../ckpt_final.pt` + `results/cqa_multitype/<run_set>/*.json` | Keep continuous multi-type alive only if `udf_distance` stays competitive with the discrete shared line **and** `mesh_normal` remains above its majority/code-proxy baseline with meaningful `no_context` / `wrong_shape_*` sensitivity on same and offdiag. | `restart_plan -> runlog -> storyline once dist+normal continuous is read` | ABCI jobs `115972-115973` completed. Shared continuous `DISTANCE + NORMAL` keeps `udf_distance` alive (`same MAE=0.0274`, `offdiag MAE=0.1372`, strong distance controls), but `mesh_normal` fails (`same mean_cos=0.0023`, `offdiag mean_cos=0.0084`, code-acc proxy below majority, controls near zero). Do **not** promote shared continuous multi-type; continuous stays task-specific for now. |
| `C025` | `32` | If `mesh_normal` is weak mainly because signed winding noise corrupts the target, does `DISTANCE + NORMAL_UNSIGNED` rescue the first shared mesh branch without changing the current discrete independent recipe? | `C` | `canonized` | `10k ABCI shared pretrain + same/offdiag suite + TYPE-switch export` | `C020-C024` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_dist_norm_unsigned{,_pcbank_eval}.yaml` + existing multitype suite / type-switch wrappers | `runs/cqa/<run_set>/.../ckpt_final.pt` + `results/cqa_multitype/<run_set>/*.json` + `results/cqa_multitype/<run_set>_typeswitch/index.json` | Promote unsigned normal rescue only if `mesh_normal_unsigned` materially widens the gap above its majority baseline on same/offdiag **without** degrading `udf_distance`, and if the TYPE-switch assets become visibly stronger than the signed-normal pack. | `restart_plan -> runlog -> storyline once unsigned-normal read lands` | ABCI jobs `116068-116070` completed. `udf_distance` stays stable (`same acc=0.3877`, `offdiag acc=0.2005`) while `mesh_normal_unsigned` is rescued relative to signed normals: same/offdiag `acc=0.5773/0.3832` vs majority `0.3066/0.3068`, with positive `no_context` / `wrong_shape_*` deltas and a much stronger TYPE-switch pack (`mean_cos` now roughly `0.46-0.68` on the first exported shapes instead of near-zero signed-normal behavior). Treat unsigned normal as the new default mesh-normal target; winding-subset filtering and AO/viscount remain follow-up options rather than blockers. |
| `C026` | `33` | If unsigned normals fix the target pathology on the discrete shared line, does the same rescue also recover the shared `DISTANCE + NORMAL` continuous branch, or is the continuous failure mode deeper than the sign ambiguity? | `C` | `canonized` | `10k ABCI shared continuous pretrain + same/offdiag suite` | `C024-C025` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_dist_norm_unsigned_continuous{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_distnorm_unsigned_continuous.sh` | `runs/cqa/<run_set>/.../ckpt_final.pt` + `results/cqa_multitype/<run_set>/*.json` | Promote unsigned continuous rescue only if `mesh_normal_unsigned` rises clearly above majority / code-proxy baseline on same and offdiag while preserving the current continuous `udf_distance` quality; if not, treat the continuous mesh weakness as a deeper optimization/interface issue rather than signed-label noise alone. | `restart_plan -> runlog -> storyline once unsigned continuous read lands` | ABCI jobs `116094-116095` completed. Unsigned normals rescue the continuous shared branch strongly on geometric quality: `mesh_normal_unsigned` moves from signed-continuous `mean_cos=0.0023/0.0084` to `0.7954/0.6829` on same/offdiag, with strong `no_context` / `wrong_shape_*` drops in `mean_cos`; `udf_distance` remains alive (`same MAE=0.0305`, `offdiag MAE=0.1229`). However, code-proxy for continuous normals still sits below the unsigned-discrete majority baseline, so treat this as a strong geometric rescue of continuous normals, not yet a reason to replace the discrete unsigned mainline. |
| `C027` | `34` | If `udf_thickness` is only blocked by zero-dominant support and fixed log bins, does a strict valid-support + quantile-bin rescue make `DISTANCE + THICKNESS` viable as the second UDF answer family? | `C` | `canonized` | `local audit + 10k ABCI shared pretrain + same/offdiag suite` | `C001`, `C004`, `C025-C026` canonized | `results/cqa_audit/patchnepa_cqa_rescue_audit_20260324/summary.json` + `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_dist_thick_valid_qbin{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_distthick_valid_shared.sh` | `runs/cqa/patchnepa_cqa_distthick_valid_shared_20260324_r1/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_distthick_valid_shared_20260324_r1_suite/cqa_distthick_valid_suite.json` | Reopen thickness only if the rescued task keeps its top-1 share well below the old `~0.66` pathology **and** the shared `DISTANCE + THICKNESS` run preserves `udf_distance` while `udf_thickness_valid_qbin` stays above majority on same/offdiag. | `restart_plan -> runlog -> storyline once rescue read lands` | Rescue succeeded. Local audit drops the old majority pathology to `majority_baseline_acc=0.0564`, `entropy_bits=5.45`, `unique_codes=64`, support rate `0.1063`. After fixing the first runtime failure (`116098`) by replacement-padding short valid pools, rerun `116102-116103` shows `udf_distance` preserved (`same/offdiag acc=0.3531/0.1890`) and `udf_thickness_valid_qbin` now above majority on same/offdiag (`0.0921/0.0449` vs `0.0241/0.0236`) with positive `no_context` / `wrong_shape_*` deltas. Treat rescued thickness as a viable second UDF-family answer, still weaker than `distance` but no longer headline-blocked by zero-dominance. |
| `C028` | `35` | If `mesh_visibility` bitpack is too brittle for headline use, is `mesh_viscount` a cheaper scalar-like mesh answer that can coexist with `DISTANCE + NORMAL_UNSIGNED` in the same shared checkpoint? | `C` | `canonized` | `local audit + 2k ABCI shared smoke + same/offdiag suite` | `C001`, `C025` canonized | `results/cqa_audit/patchnepa_cqa_rescue_audit_20260324/summary.json` + `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_dist_norm_unsigned_viscount{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_distnormviscount_shared.sh` | `runs/cqa/patchnepa_cqa_distnormviscount_shared_20260324/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_distnormviscount_shared_20260324_suite/cqa_distnormviscount_suite.json` | Keep `mesh_viscount` alive only if the short shared smoke beats its `~0.585` majority baseline on same-context while preserving the existing `DISTANCE + NORMAL_UNSIGNED` read; otherwise demote it and revisit AO/continuous alternatives. | `restart_plan -> runlog -> storyline only if viscount survives the smoke` | Negative first pass. Local audit already warned that `mesh_viscount` is majority-heavy (`majority_baseline_acc=0.5854`, `entropy_bits=1.7393`, `6` active codes). Short smoke `116100-116101` preserves the `DISTANCE + NORMAL_UNSIGNED` parts only partially, but `mesh_viscount` itself stays at or below majority (`same/offdiag acc=0.5363/0.5340` vs `0.5361/0.5364`) with nearly inert controls (`delta_ce(no_context)=+0.0925/+0.0722`). Demote `mesh_viscount` as a headline mesh-family answer and revisit AO or continuous scalar alternatives instead of promoting this first pass. |
| `C029` | `36` | If `mesh_viscount` fails as a discrete count task, does `mesh_ao` succeed as a smoother continuous scalar mesh-family answer under the same same/offdiag control protocol? | `C` | `canonized` | `10k ABCI continuous pretrain + same/offdiag suite` | `C025-C028` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_mesh_ao_continuous{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_meshao_continuous.sh` | `runs/cqa/patchnepa_cqa_meshao_continuous_20260324_142309/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_meshao_continuous_20260324_142309_r1_suite/cqa_meshao_continuous_suite.json` | Keep AO alive only if same/offdiag controls show clear context dependence (`delta_mae(no_context) > 0`, `wrong_shape_other > wrong_shape_same`) without obvious scalar collapse (`pred_std` not near-zero). | `restart_plan -> runlog -> storyline once AO read lands` | Positive first pass. Train `116359` completed; initial suite `116360` only exposed a formatting bug (`mesh_ao` has no `code_acc_proxy`) and was rerun as `116362`. Final read: same/offdiag `MAE=0.1821/0.1985`, `pred_std=0.0681/0.0658` vs target `0.2147/0.2150`, with positive `no_context` deltas (`+0.0334/+0.0164`) and `wrong_shape_other > wrong_shape_same` (`0.0322>0.0220`, `0.0185>0.0145`). Treat `mesh_ao` as a viable smooth scalar mesh-family candidate and the best next mesh-side answer after `normal_unsigned`; next natural gate is shared `DISTANCE + NORMAL_UNSIGNED + AO`. |
| `C030` | `37` | If `mesh_ao` is viable alone, does a shared continuous `DISTANCE + NORMAL_UNSIGNED + AO` checkpoint preserve the current UDF + mesh reads while adding a second mesh-family answer? | `C` | `canonized` | `10k ABCI shared continuous pretrain + same/offdiag suite` | `C025-C029` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_dist_norm_unsigned_ao_continuous{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_distnormao_continuous.sh` | `runs/cqa/patchnepa_cqa_distnormao_continuous_20260324_181908/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_distnormao_continuous_20260324_181908_suite/cqa_distnormao_continuous_suite.json` | Keep the 3-type AO branch alive only if `udf_distance` and `mesh_normal_unsigned` do not collapse relative to their current single/shared baselines, while `mesh_ao` remains context-sensitive (`delta_mae(no_context) > 0`, `wrong_shape_other > wrong_shape_same`). | `restart_plan -> runlog -> storyline once 3-type AO read lands` | Positive first pass. Train `116610` and suite `116611` completed cleanly. Same-context regresses slightly versus the best `DISTANCE + NORMAL_UNSIGNED` continuous pair (`udf_distance MAE=0.0360` vs `0.0305`, `mesh_normal_unsigned mean_cos=0.7776` vs `0.7954`), but off-diagonal is preserved or improved (`udf_distance MAE=0.0948`, `IoU@0.05=0.4858`; `mesh_normal_unsigned mean_cos=0.6828`). `mesh_ao` also survives co-training (`same/offdiag MAE=0.1927/0.1988`) with positive controls (`delta_mae(no_context)=+0.0283/+0.0209`, `wrong_shape_other > wrong_shape_same`). Treat this as the first surviving mesh-two-answer shared line; keep it alive, but still regard the discrete `DISTANCE + NORMAL_UNSIGNED` branch as the safer publishable multi-type anchor. |
| `C031` | `38` | Is the small same-context regression in shared continuous `DISTANCE + NORMAL_UNSIGNED + AO` mainly a task-loss scale problem, and do `ema_norm` or fixed task weights improve the tradeoff? | `C` | `canonized` | `2x 10k ABCI shared continuous reruns + same/offdiag suite` | `C030` canonized | `nepa3d/tracks/patch_nepa/cqa/train/pretrain_distnorm_continuous.py` + `scripts/abci/submit_patchnepa_current_cqa_distnormao_continuous_{emanorm,fixedw}.sh` | `runs/cqa/patchnepa_cqa_distnormao_continuous_{emanorm,fixedw}_*/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_distnormao_continuous_{emanorm,fixedw}_*_suite/*.json` | Keep a balanced-loss variant only if it materially recovers same-context `udf_distance`/`mesh_normal_unsigned` without collapsing `mesh_ao` or sacrificing the off-diagonal gain from `C030`. | `restart_plan -> runlog -> storyline once the balanced-loss comparison lands` | Positive but mixed. `ema_norm` (`116671/116673`) and fixed weights (`116670/116672`) both recover same-context `udf_distance` relative to baseline `C030` (`MAE 0.0155 / 0.0206` vs `0.0360`) while keeping `mesh_ao` alive, but both sacrifice the off-diagonal gain of the unbalanced baseline (`offdiag udf_distance MAE 0.1295 / 0.1215` vs `0.0948`; `mesh_normal_unsigned mean_cos 0.6212 / 0.6439` vs `0.6828`). Fixed weights are the safer of the two balanced variants, but current safest read is still: raw shared AO branch for offdiag robustness, balanced variants as supportive evidence that the same-context regression is indeed a task-scale issue. |
| `C032` | `39` | If rescued `udf_thickness_valid_qbin` is already viable at 64 bins, does raising only the rescued thickness branch to 128 quantile bins (`cqa_v3`) improve the thickness ceiling without breaking the current UDF read? | `C` | `canonized` | `local audit + 10k ABCI thickness-only + 10k ABCI dist+thick same/offdiag suite` | `C027`, `C029-C031` canonized | `nepa3d/tracks/patch_nepa/cqa/data/cqa_codec.py` + `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v3_{thick,dist_thick}{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v3_{thick128_single,distthick128}.sh` | `results/cqa_audit/patchnepa_cqa_v3_thick128_audit_20260325/summary.json` + `runs/cqa/patchnepa_cqa_v3_*/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_v3_*_suite/*.json` | Promote `cqa_v3` only if `thickness@128` keeps majority far below the old pathology, improves or preserves thickness same/offdiag over the 64-bin rescue, and does not materially degrade `udf_distance` in the shared `dist+thick` run. | `restart_plan -> runlog -> storyline once cqa_v3 thickness128 lands` | Mixed / negative. Local `cqa_v3` audit is very healthy (`majority_baseline_acc=0.01068`, `entropy_bits=6.99`, `unique_codes=128`), and thickness-only training `117019-117022` stays above majority on same/offdiag (`acc=0.1792/0.1495` vs `0.1365/0.1362`) with positive `no_context` / `wrong_shape_*` deltas. But the shared `dist+thick` rerun `117020-117021` is worse than the current 64-bin rescue: `udf_distance` drops sharply (`same/offdiag acc=0.1519/0.0722` vs `0.3531/0.1890` in `C027`), and `udf_thickness_valid_qbin` only improves superficially because the eval majority baseline rises (`same/offdiag acc=0.1678/0.1233` vs majority `0.1365/0.1362`). Do not promote `cqa_v3` as the new mainline; keep `cqa_v2` thickness-64 as the safer canonical UDF branch. |
| `C033` | `40` | If the current CQA story is mainly about the Q/A interface rather than a shared prefix stack, does a discrete `cqa_v2` encoder-decoder branch (`DISTANCE + NORMAL_UNSIGNED`) match or improve the current prefixlm baseline on same/offdiag token eval, distance completion, and encoder-only utility classification? | `C` | `canonized` | `10k ABCI enc-dec pretrain + dependent suite/completion/utility row` | `C025`, `C032` canonized | `nepa3d/core/models/encdec_transformer.py` + `nepa3d/tracks/patch_nepa/cqa/models/{primitive_answering_encdec.py,factory.py}` + `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_unsigned{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnorm_unsigned_encdec.sh` | `runs/cqa/patchnepa_cqa_v2_distnorm_unsigned_encdec_20260325_173546/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_v2_distnorm_unsigned_encdec_20260325_173546_suite/cqa_v2_distnorm_unsigned_encdec_suite.json` + `results/cqa_completion/patchnepa_cqa_v2_distnorm_unsigned_encdec_20260325_173546_{same,offdiag}_completion/*.json` + `runs/cqa_cls/patchnepa_cqa_v2_distnorm_unsigned_encdec_20260325_173546_*_cls/*/ckpt_best.pt` | Promote encoder-decoder only if it at least matches the current prefixlm `cqa_v2 DISTANCE + NORMAL_UNSIGNED` line on token/completion while preserving paper-safe utility; if it only ties, keep it as supportive architecture evidence, and if it drops, retain prefixlm as mainline. | `restart_plan -> runlog -> storyline once the enc-dec compare pack lands` | ABCI jobs `117334-117340` completed. The enc-dec branch is scientifically alive but clearly below the prefixlm mainline on the first compare. Token eval: same/offdiag `udf_distance acc=0.0981/0.0501` and `mesh_normal_unsigned acc=0.4922/0.3917`, versus prefixlm unsigned baseline `0.3877/0.2005` and `0.5773/0.3832`; only offdiag `mesh_normal_unsigned` is roughly tied/slightly higher. Distance completion is also worse than the canonical prefixlm `udf_distance` reference: same/offdiag `MAE=0.0437/0.1061`, `IoU@0.05=0.5012/0.3362`, `mesh_fscore=0.0470/0.0252` versus `0.0207/0.1239`, `0.7537/0.3861`, `0.1702/0.1014`. Utility remains paper-safe but lower than `C014`: `obj_bg=0.8038`, `obj_only=0.8158`, `pb_t50_rs=0.7595` versus `0.8399`, `0.8468`, `0.7765`. Keep prefixlm as mainline; treat enc-dec as a negative-but-informative architecture ablation showing that the current gains do not come for free from a T5-style separation. |
| `C034` | `40` | For a strict architecture comparison, does a rerun of the current strongest prefixlm unsigned-normal branch under the same `cqa_v2` codec and the same dependent token/completion/utility protocol materially change the read against `C033`, or does the first enc-dec loss remain decisively architecture-side? | `C` | `canonized` | `10k ABCI prefixlm pretrain + dependent suite/completion/utility row` | `C033` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_unsigned{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnorm_unsigned_prefixlm.sh` | `runs/cqa/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_20260325_234124/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_20260325_234124_suite/*.json` + `results/cqa_completion/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_20260325_234124_{same,offdiag}_completion/*.json` + `runs/cqa_cls/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_20260325_234124_*_cls/*/ckpt_best.pt` | Promote a reinterpretation of `C033` only if strict `prefixlm + cqa_v2` falls much closer to enc-dec than the older unsigned baseline; if prefixlm still clearly wins, freeze the architecture read and stop attributing the gap to codec mismatch. | `restart_plan -> runlog -> storyline once strict prefixlm cqa_v2 compare lands` | ABCI jobs `117412-117418` completed. Strict `prefixlm + cqa_v2` remains clearly stronger than the enc-dec branch, so the `C033` read was not a codec-version artifact. Token eval: same/offdiag `udf_distance acc=0.1745/0.0797`, `mesh_normal_unsigned acc=0.5495/0.3566`; compared with enc-dec `0.0981/0.0501` and `0.4922/0.3917`, prefixlm wins on both `udf_distance` reads and same-context `mesh_normal_unsigned`, while enc-dec only keeps a small edge on offdiag `mesh_normal_unsigned`. Distance completion also favors prefixlm overall: same/offdiag `MAE=0.0192/0.1249`, `IoU@0.05=0.7030/0.3814`, `mesh_fscore=0.1196/0.0700`, versus enc-dec `0.0437/0.1061`, `0.5012/0.3362`, `0.0470/0.0252`. Utility uses the same `pointmae(test-as-val)` protocol and also favors prefixlm: `obj_bg=0.8399`, `obj_only=0.8503`, `pb_t50_rs=0.7679` versus enc-dec `0.8038/0.8158/0.7595`. Freeze the architecture read: prefixlm remains the safer mainline and enc-dec stays a negative-but-informative ablation. |
| `C035` | `41` | Under the current raw targets and the unified discrete `cqa_v2` codec, can a 4-task shared prefixlm checkpoint (`DISTANCE + NORMAL_UNSIGNED + THICKNESS_VALID_QBIN + AO`) survive same/offdiag token eval, distance completion, and utility classification strongly enough to serve as the current multi-probe ceiling before any AO-target rebuild? | `C` | `canonized` | `10k ABCI prefixlm pretrain + dependent suite/completion/utility row` | `C027`, `C029`, `C034` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_thick_ao{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnormthickao_prefixlm.sh` | `runs/cqa/patchnepa_cqa_v2_distnormthickao_prefixlm_20260326_042609/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_v2_distnormthickao_prefixlm_20260326_042609_suite/cqa_v2_distnormthickao_prefixlm_suite.json` + `results/cqa_completion/patchnepa_cqa_v2_distnormthickao_prefixlm_20260326_042609_{same,offdiag}_completion/*.json` + `runs/cqa_cls/patchnepa_cqa_v2_distnormthickao_prefixlm_20260326_042609_*_cls/*/ckpt_best.pt` | Promote the 4-task line only if `udf_distance` and `mesh_normal_unsigned` remain paper-safe relative to the current prefixlm `cqa_v2 DISTANCE + NORMAL_UNSIGNED` anchor while `udf_thickness_valid_qbin` and `mesh_ao` both stay context-sensitive above their majority-heavy baselines; otherwise treat the run as the current raw-target ceiling and use it to decide whether AO HQ target rebuild is worth the next wave. | `restart_plan -> runlog -> storyline once the 4-task pack lands` | ABCI jobs `117455-117461` completed. The 4-task branch survives but does not show positive synergy. Token eval remains alive on all tasks, but `udf_distance` and `mesh_normal_unsigned` regress relative to the current 2-task anchor: same/offdiag `udf_distance acc=0.1232/0.0601` and `mesh_normal_unsigned acc=0.5091/0.3841`, versus `0.1745/0.0797` and `0.5495/0.3566` in `C034`. `udf_thickness_valid_qbin` stays above majority (`0.0765/0.0465` vs `0.0241/0.0236`) with positive controls, but `mesh_ao` remains at/below majority (`0.5523/0.5242` vs `0.5550/0.5566`) despite positive `no_context` deltas. Distance completion also trades off rather than improving the anchor: same/offdiag `MAE=0.0290/0.1161`, `IoU@0.05=0.6320/0.3583`, `mesh_fscore=0.0759/0.0422`, compared with `C034` `0.0192/0.1249`, `0.7030/0.3814`, `0.1196/0.0700`; only offdiag MAE improves slightly. Utility classification is slightly stronger under the same `pointmae(test-as-val)` protocol (`obj_bg=0.8451`, `obj_only=0.8520`, `pb_t50_rs=0.7710`), but not enough to offset the main token/completion regressions. Treat this as the current raw-target ceiling: complementary probes are real, but at 10k the branch shows tradeoff rather than monotonic synergy, and the next targeted improvement should be AO-HQ rather than further mainline rewiring. |
| `C036` | `42` | Can additive raw-target augmentation on a derived eval subset (`AO HQ` and `HKS`) complete cleanly enough to justify the next target-quality wave without mutating the frozen `world_v3` cache contract? | `B` | `canonized` | `2x CPU subset-smoke augment jobs on derived cache roots` | `C035` canonized | `nepa3d/data/augment_world_v3_mesh_aux.py` + `scripts/preprocess/{augment,submit}_world_v3_mesh_aux_{qc,sharded_qc}.sh` | `results/data_freeze/world_v3_{aohq,hks}_smoke_eval64_sharded/mesh_aux_summary.json` + derived cache roots under `data/shapenet_unpaired_cache_v2_20260311_worldvis_drop1_{aohq,hks}_smoke_eval64_sharded` | Keep AO-HQ/HKS alive only if the derived-cache augmentation finishes with low error rate on the subset and produces the expected additive fields (`mesh_surf_ao_hq*`, `mesh_surf_hks_t*`) without touching the canonical cache root; otherwise fix the raw-target pipeline before any new CQA smoke. | `restart_plan -> runlog -> storyline once the derived-cache smoke is interpreted` | Canonized from the sharded CPU smoke. AO-HQ is clearly positive on the derived 64-shape eval subset: all `64/64` NPZs received `mesh_surf_ao_hq`, the canonical cache root stayed untouched, and the field support becomes much denser than the old 8-ray target (`old mesh_surf_ao`: `7` unique rounded levels, `std=0.219`; `mesh_surf_ao_hq`: `129` rounded levels, `std=0.433`). HKS is only partially successful: the sharded smoke yields `mesh_surf_hks_t0` on `48/64` subset shapes, with repeated ARPACK failures on harder meshes and one missing shard summary. Decision: AO-HQ is the next target-quality wave; HKS stays smoke-only until the eigensolver path is stabilized. The obsolete serial HKS smoke `117465` was cancelled after the sharded read landed. |
| `C037` | `43` | If the first enc-dec branch mainly lost on shallow query-conditioned computation, does simply matching the decoder depth to the encoder (`decoder_layers=12`) recover the `cqa_v2 DISTANCE + NORMAL_UNSIGNED` read without changing the no-Q interface? | `C` | `canonized` | `10k ABCI enc-dec d12/no_q pretrain + dependent suite/completion/utility row` | `C033`, `C034` canonized | `nepa3d/tracks/patch_nepa/cqa/models/primitive_answering_encdec.py` + `nepa3d/core/models/encdec_transformer.py` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnorm_unsigned_encdec_d12_noq.sh` | `runs/cqa/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_noq_*/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_noq_*_suite/*.json` + `results/cqa_completion/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_noq_*_{same,offdiag}_completion/*.json` + `runs/cqa_cls/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_noq_*_cls/*/ckpt_best.pt` | Promote the deeper no-Q enc-dec variant only if it closes most of the gap to the strict prefixlm `cqa_v2` anchor on `udf_distance` token/completion while preserving utility, thereby isolating shallow decoder depth as the main cause of `C033`. | `restart_plan -> runlog -> storyline once d12/no_q lands` | Canonized as a negative architecture follow-up. `decoder_layers=12` does not rescue the branch: same/offdiag token acc stay at `udf_distance = 0.0837 / 0.0497`, `mesh_normal_unsigned = 0.4853 / 0.3927`, and completion remains weak (`MAE/IoU@0.05/F-score = 0.0547 / 0.4575 / 0.0340` same, `0.1078 / 0.3239 / 0.0204` offdiag). Utility is `0.7762 / 0.8158 / 0.7595`, below the strict prefixlm `cqa_v2` anchor. Read: shallow decoder depth was not the main cause of `C033`. |
| `C038` | `44` | If query-context precomputation was the missing ingredient in enc-dec, does restoring an explicit decoder-side Q block (`query_interface_mode=full_q`) on top of a 12-layer decoder recover the `cqa_v2 DISTANCE + NORMAL_UNSIGNED` read? | `C` | `canonized` | `10k ABCI enc-dec d12/full_q pretrain + dependent suite/completion/utility row` | `C033`, `C034`, `C037` canonized | `nepa3d/tracks/patch_nepa/cqa/models/primitive_answering_encdec.py` + `nepa3d/core/models/encdec_transformer.py` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnorm_unsigned_encdec_d12_fullq.sh` | `runs/cqa/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_fullq_*/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_fullq_*_suite/*.json` + `results/cqa_completion/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_fullq_*_{same,offdiag}_completion/*.json` + `runs/cqa_cls/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_fullq_*_cls/*/ckpt_best.pt` | Promote the deeper full-Q enc-dec variant only if it materially beats the shallower/no-Q enc-dec rows and moves closer to the prefixlm `cqa_v2` anchor, especially on `udf_distance` same-context token/completion. | `restart_plan -> runlog -> storyline once d12/full_q lands` | Canonized as a second negative architecture follow-up. Reintroducing a decoder-side Q block improves offdiag `mesh_normal_unsigned` to `0.4189`, but does not recover the main `udf_distance` read (`0.0801 / 0.0503`) or completion (`MAE/IoU@0.05/F-score = 0.0589 / 0.4481 / 0.0298` same, `0.1121 / 0.3033 / 0.0190` offdiag). Utility also stays below the strict prefixlm `cqa_v2` anchor at `0.7762 / 0.8090 / 0.7491`. Read: decoder-side query tokens help mesh-normal transfer slightly, but they do not close the prefixlm gap. |
| `C039` | `45` | Do frozen query-conditioned CQA representations already encode an unseen mesh-local scalar strongly enough that a linear probe can recover `curvature` from `answer_hidden` under the same same/offdiag/control protocol? | `B` | `canonized` | `2x ABCI frozen linear-probe jobs` | `C034`, `C035` canonized | `nepa3d/tracks/patch_nepa/cqa/analysis/train_frozen_geometric_probe.py` + `scripts/{eval/nepa3d_cqa_geo_probe_qg.sh,abci/submit_patchnepa_current_cqa_geo_probe.sh}` + `C034/C035` ckpts | `results/cqa_probe/patchnepa_cqa_probe_curvature_{c034,c035}_*/cqa_probe_curvature_*.json` + `runs/cqa_probe/patchnepa_cqa_probe_curvature_{c034,c035}_*/cqa_probe_curvature_*/ckpt_best.pt` | Treat curvature as positive evidence only if the frozen probe beats the scalar mean baseline on same-context, keeps useful signal off-diagonal, and degrades under `no_context` / wrong-shape controls rather than acting like a context-free readout. | `restart_plan -> runlog -> storyline once curvature probe lands` | Canonized. Probe source is `answer_hidden`, not encoder-only pooling. `C034` (`117619`) and `C035` (`117622`) both beat the scalar mean baseline on same-context curvature with positive controls: `C034 same MAE/RMSE/r = 0.1482 / 0.1988 / 0.4821` vs mean baseline `0.1870 / 0.2219`; `C035 same = 0.1517 / 0.1999 / 0.4514` vs the same baseline. Off-diagonal is weaker and only partially positive: `C034` is still above the mean-baseline MAE (`0.2000` vs `0.1889`), while `C035` nearly matches it (`0.1888` vs `0.1889`) and improves Pearson `r` to `0.2210`. The clean read is that unseen scalar curvature is linearly recoverable from frozen query-conditioned representations on same-context and remains context-sensitive, but the off-diagonal probe is only moderate rather than fully strong. |
| `C040` | `46` | On a winding-consistent subset only, can the same frozen CQA representations recover `signed normal` with a linear probe even though the mainline was trained only on unsigned normals? | `B` | `canonized` | `2x ABCI frozen linear-probe jobs + additive subset manifest` | `C034`, `C035` canonized; signed target restricted to `is_winding_consistent=1` | `nepa3d/tracks/patch_nepa/cqa/analysis/train_frozen_geometric_probe.py` + `scripts/{eval/nepa3d_cqa_geo_probe_qg.sh,abci/submit_patchnepa_current_cqa_geo_probe.sh}` + `results/cqa_probe/patchnepa_cqa_probe_signed_subset_20260326/winding_consistent_subset.json` | `results/cqa_probe/patchnepa_cqa_probe_signed_{c034,c035}_*/cqa_probe_signed_*.json` + `runs/cqa_probe/patchnepa_cqa_probe_signed_{c034,c035}_*/cqa_probe_signed_*/ckpt_best.pt` | Treat signed-normal probe as positive only if same-context `mean_cos` is materially above zero on the winding-consistent subset and still drops under `no_context` / wrong-shape controls; otherwise keep the result as target-pathology-limited or non-emergent. | `restart_plan -> runlog -> storyline once signed-normal probe lands` | Canonized as a negative analysis result. The additive winding-consistent subset manifest is reusable, but even there the frozen linear probe does not recover signed orientation: `C034 same/offdiag mean_cos = 0.0094 / 0.0032`, `C035 = 0.0090 / 0.0049`, all at roughly `89.3-89.7 deg`. Control behavior is also inconsistent, with several wrong-shape and `no_context` conditions improving rather than degrading off-diagonal cosine. Read: current CQA representations do not linearly expose signed normal, even when full-corpus winding noise is filtered out. |
| `C041` | `47` | Can AO-HQ be added over the full `train_mesh + eval` mesh splits as an additive derived cache, without mutating canonical `world_v3`, so that a real discrete rerun can use `mesh_surf_ao_hq` instead of the current coarse AO target? | `C` | `canonized` | `sharded CPU AO-HQ augmentation on derived cache root` | `C036` canonized | `nepa3d/data/augment_world_v3_mesh_aux.py` + `scripts/preprocess/submit_world_v3_aohq_fullmesh_sharded_qc.sh` | `data/shapenet_unpaired_cache_v2_20260311_worldvis_drop1_aohq_fullmesh_sharded_r3` + `results/data_freeze/world_v3_aohq_fullmesh_r3_20260327_041356/mesh_aux_summary.json` + shard logs under `logs/preprocess/world_v3_mesh_aux/` | Promote the derived AO-HQ cache only if both `train_mesh` and `eval` finish with low error rate and `mesh_surf_ao_hq` exists broadly enough to support a proper discrete rerun, while canonical `world_v3` remains untouched. | `restart_plan -> runlog -> storyline once fullmesh AO-HQ build lands` | Canonized. The first fullmesh wave hit widespread OOM; the second avoided OOM but was too slow. The canonical `r3` wave enforced `ray_pyembree` with no fallback, kept batch-local AO direction construction, and finished cleanly on the full additive derived cache: `train_mesh 16004/16004`, `eval 5241/5241`, `overall 21245/21245`, `0` errors, `AO_RAYS=128`, `AO_BATCH_SIZE=64`. Canonical `world_v3` stayed untouched; the AO-HQ reruns can now be treated as real target-quality experiments rather than subset-only smoke. |
| `C042` | `48` | Once AO-HQ exists on a derived cache, does discrete `cqa_v2 DISTANCE + NORMAL_UNSIGNED + AO_HQ` recover a headline-safe second mesh-family answer better than the current raw-AO branch? | `C` | `canonized` | `10k ABCI prefixlm pretrain + dependent suite/completion/utility row` | `C030`, `C036`, `C041` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_aohq{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnormaohq_prefixlm.sh` | `runs/cqa/patchnepa_cqa_v2_distnormaohq_prefixlm_20260327_041356_r3/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_v2_distnormaohq_prefixlm_20260327_041356_r3_suite/cqa_v2_distnormaohq_prefixlm_suite.json` + `results/cqa_completion/patchnepa_cqa_v2_distnormaohq_prefixlm_20260327_041356_r3_{same,offdiag}_completion/*.json` + `runs/cqa_cls/patchnepa_cqa_v2_distnormaohq_prefixlm_20260327_041356_r3_*_cls/*/ckpt_best.pt` | Promote AO-HQ only if `mesh_ao_hq` finally beats its majority baseline on same/offdiag while preserving the current `DISTANCE + NORMAL_UNSIGNED` token/completion anchor closely enough to be paper-safe. | `restart_plan -> runlog -> storyline once AO-HQ 3-task rerun lands` | Canonized as a supportive-but-not-headline-safe target upgrade. `mesh_ao_hq` clears majority only very slightly on same-context (`0.5650` vs `0.5624`) and still fails off-diagonal (`0.5018` vs `0.5535`), although controls remain strongly positive (`no_context delta_token_acc = -0.4456 / -0.3779` same/offdiag). The anchor tasks stay alive but do not beat the current `C034` 2-task mainline: token acc same/offdiag `udf_distance = 0.1464 / 0.0688`, `mesh_normal_unsigned = 0.5288 / 0.3808`; completion `MAE = 0.0227 / 0.1214`, `IoU@0.05 = 0.6776 / 0.3657`, `mesh_fscore = 0.0999 / 0.0562` versus `C034` `0.0192 / 0.1249`, `0.7030 / 0.3814`, `0.1196 / 0.0700`. Utility is strong (`obj_bg=0.8520`, `obj_only=0.8485`, `pb_t50_rs=0.7734`), but AO-HQ does not yet make the mesh-side second answer paper-safe on off-diagonal transfer. |
| `C043` | `49` | If AO-HQ fixes the raw target quality bottleneck, does the 4-task discrete line (`DISTANCE + NORMAL_UNSIGNED + THICKNESS_VALID_QBIN + AO_HQ`) move beyond the current raw-target ceiling and become a real multi-probe candidate? | `C` | `canonized` | `10k ABCI prefixlm pretrain + dependent suite/completion/utility row` | `C035`, `C041`, `C042` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_thick_aohq{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnormthickaohq_prefixlm.sh` | `runs/cqa/patchnepa_cqa_v2_distnormthickaohq_prefixlm_20260327_041356_r3/.../ckpt_final.pt` + `results/cqa_multitype/patchnepa_cqa_v2_distnormthickaohq_prefixlm_20260327_041356_r3_suite/cqa_v2_distnormthickaohq_prefixlm_suite.json` + `results/cqa_completion/patchnepa_cqa_v2_distnormthickaohq_prefixlm_20260327_041356_r3_{same,offdiag}_completion/*.json` + `runs/cqa_cls/patchnepa_cqa_v2_distnormthickaohq_prefixlm_20260327_041356_r3_*_cls/*/ckpt_best.pt` | Promote the AO-HQ 4-task line only if it clearly improves over `C035` on the AO branch without sacrificing the current `udf_distance` / `mesh_normal_unsigned` anchor more than the existing 4-task tradeoff. | `restart_plan -> runlog -> storyline once AO-HQ 4-task rerun lands` | Canonized as a modest improvement over the raw-AO 4-task ceiling, but still not a new mainline. AO-HQ improves the mesh-side branch only slightly: same/offdiag `mesh_ao_hq = 0.5624 / 0.5289` versus raw `mesh_ao = 0.5523 / 0.5242`, yet it remains at/below majority (`0.5624 / 0.5535`) and therefore not headline-safe. The other probes remain alive: `udf_distance = 0.1144 / 0.0613`, `mesh_normal_unsigned = 0.5021 / 0.4005`, `udf_thickness_valid_qbin = 0.0778 / 0.0504`, all above their majorities, with positive control drops. Completion remains tradeoff-shaped: same/offdiag `MAE = 0.0324 / 0.1066`, `IoU@0.05 = 0.5974 / 0.3402`, `mesh_fscore = 0.0604 / 0.0388`; this improves offdiag MAE relative to raw-AO `C035` (`0.1161 -> 0.1066`) but weakens most other completion metrics. Utility is mixed but paper-safe (`obj_bg=0.8434`, `obj_only=0.8468`, `pb_t50_rs=0.7797`). Read: AO-HQ helps, especially on stability and some offdiag utility, but the 4-task branch still shows tradeoff rather than a clear synergistic mainline gain. |
| `C044` | `50` | Under degraded context inputs, does the current safest mainline `C034` (`DISTANCE + NORMAL_UNSIGNED`) degrade more gracefully than classical point-to-mesh baselines, giving a stronger robustness-style result than the current clean completion-only story? | `C` | `canonized` | `1 ABCI degraded suite over same/offdiag, dropout+noise, Open3D baselines` | `C034` canonized | `runs/cqa/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_20260325_234124/.../ckpt_final.pt` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnorm_unsigned_prefixlm_degraded.sh` + `nepa3d/tracks/patch_nepa/cqa/analysis/completion_udfdist_degraded_cqa.py` | `results/cqa_degraded/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_degraded_20260327_190535/cqa_v2_distnorm_unsigned_prefixlm_degraded_suite.{json,csv,md}` + exported visual subset under the same run root | Promote as a new headline axis only if CQA stays competitive or clearly better than Poisson/BPA as dropout/noise intensify, especially under offdiag, while retaining a readable graceful-degradation curve. | `restart_plan -> runlog -> storyline once degraded suite lands` | Canonized as a negative-but-useful robustness read. The v1 suite completed on `16` shapes for `same + offdiag`, `clean + dropout{0.50,0.25,0.10,0.05} + gaussian{0.01,0.02,0.05}`. CQA field metrics degrade smoothly (`same mae 0.0190 -> 0.0658` from clean to `dropout@0.05`, offdiag `0.1269 -> 0.1492`; `same iou@0.05 0.2828 -> 0.1336`, offdiag `0.1634 -> 0.1226`), but the headline mesh comparison is clearly negative: BPA wins `mesh_fscore` across clean and degraded conditions (`same clean 0.3193`, `offdiag clean 0.2409`, versus CQA `0.0176 / 0.0194`), and Poisson also beats CQA on the mesh-facing rows. Read: the first degraded-input wave does not yet provide the desired killer robustness result; it should be treated as a completed negative control / protocol prototype rather than a promoted headline axis. |
| `C045` | `51` | If the current safest prefixlm discrete mainline is allowed to run to a real epoch-driven budget, does `cqa_v2 DISTANCE + NORMAL_UNSIGNED` remain the best core branch once completion, utility classification, and frozen geometric probes are all reevaluated from a full ckpt? | `C` | `deferred` | `100-epoch ABCI prefixlm pretrain + dependent suite/completion/utility/probe chain` | `C034`, `C039`, `C040`, `C044` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_unsigned{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnorm_unsigned_prefixlm_full.sh` | `runs/cqa/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_full_20260327_192122/.../ckpt_final.pt` + dependent outputs under `results/{cqa_multitype,cqa_completion,cqa_probe}/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_full_20260327_192122*` + `runs/cqa_cls/patchnepa_cqa_v2_distnorm_unsigned_prefixlm_full_20260327_192122_*_cls/*/ckpt_best.pt` | Keep this as the full-budget anchor unless a longer run reverses the current `C034` read on token/completion or collapses the utility/probe behavior. | `restart_plan -> runlog -> storyline once the full chain lands` | The full-budget wave was intentionally stopped on 2026-03-29 before scientific readout. Reason: the current mixture-sampling protocol confounds multi-task synergy/interference with per-task update frequency, so `2-type vs 4-type` full-budget conclusions are not yet reviewer-safe. Reopen only after choosing a fairer compare, e.g. matched per-task budget (`4-type @ 200ep` vs `2-type @ 100ep`) or a redesigned simultaneous multi-head protocol. |
| `C046` | `52` | Does the missing bridge branch `cqa_v2 DISTANCE + NORMAL_UNSIGNED + THICKNESS_VALID_QBIN` become a cleaner full-budget mainline candidate than the current 2-task anchor or the 4-task ceiling? | `C` | `deferred` | `100-epoch ABCI prefixlm pretrain + dependent suite/completion/utility/probe chain` | `C034`, `C028`, `C039`, `C040` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_thick{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnormthick_prefixlm_full.sh` | `runs/cqa/patchnepa_cqa_v2_distnormthick_prefixlm_full_20260327_192330/.../ckpt_final.pt` + dependent outputs under `results/{cqa_multitype,cqa_completion,cqa_probe}/patchnepa_cqa_v2_distnormthick_prefixlm_full_20260327_192330*` + `runs/cqa_cls/patchnepa_cqa_v2_distnormthick_prefixlm_full_20260327_192330_*_cls/*/ckpt_best.pt` | Promote only if the longer run shows the rescued thickness probe adding paper-safe value without eroding the current `distance + normal` anchor more than the 10k tradeoff read. | `restart_plan -> runlog -> storyline once the full chain lands` | Deferred together with `C045` for the same budget-confound reason. The queued jobs were cancelled before any readout was taken. |
| `C047` | `53` | After AO-HQ target upgrade, does a full-budget `cqa_v2 DISTANCE + NORMAL_UNSIGNED + AO_HQ` checkpoint finally turn AO into a paper-safe second mesh-family answer, or does the current supportive-only read persist? | `C` | `deferred` | `100-epoch ABCI prefixlm pretrain + dependent suite/completion/utility/probe chain` | `C042`, `C041` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_aohq{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnormaohq_prefixlm_full.sh` | `runs/cqa/patchnepa_cqa_v2_distnormaohq_prefixlm_full_20260327_192330/.../ckpt_final.pt` + dependent outputs under `results/{cqa_multitype,cqa_completion,cqa_probe}/patchnepa_cqa_v2_distnormaohq_prefixlm_full_20260327_192330*` + `runs/cqa_cls/patchnepa_cqa_v2_distnormaohq_prefixlm_full_20260327_192330_*_cls/*/ckpt_best.pt` | Promote only if full-budget AO-HQ clears its majority problem more convincingly, especially offdiag, while keeping the `distance + normal` anchor paper-safe. | `restart_plan -> runlog -> storyline once the full chain lands` | Deferred together with `C045` for the same budget-confound reason. The queued jobs were cancelled before any readout was taken. |
| `C048` | `54` | Does the current 4-probe AO-HQ ceiling (`DISTANCE + NORMAL_UNSIGNED + THICKNESS_VALID_QBIN + AO_HQ`) become a real synergistic candidate under a full-budget run, or remain a utility-supportive tradeoff branch? | `C` | `deferred` | `100-epoch ABCI prefixlm pretrain + dependent suite/completion/utility/probe chain` | `C043`, `C035`, `C041` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_thick_aohq{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnormthickaohq_prefixlm_full.sh` | `runs/cqa/patchnepa_cqa_v2_distnormthickaohq_prefixlm_full_20260327_192330/.../ckpt_final.pt` + dependent outputs under `results/{cqa_multitype,cqa_completion,cqa_probe}/patchnepa_cqa_v2_distnormthickaohq_prefixlm_full_20260327_192330*` + `runs/cqa_cls/patchnepa_cqa_v2_distnormthickaohq_prefixlm_full_20260327_192330_*_cls/*/ckpt_best.pt` | Promote only if the full run moves the 4-task branch beyond the current ceiling read and yields clear net gains rather than the existing tradeoff pattern. | `restart_plan -> runlog -> storyline once the full chain lands` | Deferred together with `C045` for the same budget-confound reason. The queued jobs were cancelled before any readout was taken. |
| `C049` | `55` | If enc-dec is given one fair full-budget chance, does the best-effort `d12/full_q` variant remain below prefixlm on the strict `cqa_v2 DISTANCE + NORMAL_UNSIGNED` compare, or does the architecture read soften at full scale? | `C` | `deferred` | `100-epoch ABCI enc-dec pretrain + dependent suite/completion/utility/probe chain` | `C038`, `C034` canonized | `nepa3d/configs/shapenet_unpaired_mix_v2_cqa_v2_dist_norm_unsigned{,_pcbank_eval}.yaml` + `scripts/abci/submit_patchnepa_current_cqa_v2_distnorm_unsigned_encdec_d12_fullq_full.sh` | `runs/cqa/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_fullq_full_20260327_192330/.../ckpt_final.pt` + dependent outputs under `results/{cqa_multitype,cqa_completion,cqa_probe}/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_fullq_full_20260327_192330*` + `runs/cqa_cls/patchnepa_cqa_v2_distnorm_unsigned_encdec_d12_fullq_full_20260327_192330_*_cls/*/ckpt_best.pt` | Keep enc-dec as a negative-but-informative architecture ablation unless the full-budget run closes most of the gap to the full prefixlm anchor on token/completion while preserving utility/probe behavior. | `restart_plan -> runlog -> storyline once the full chain lands` | Deferred together with `C045` for the same budget-confound reason. The queued jobs were cancelled before any readout was taken. |
| `L001` | `10` | Does the `g2` gain carry over to mini-CPAC on the current best mixed-source ckpt? | `B` | `queued` | `<=2h` | none | `runs/patchnepa_tokens/patchnepa_recong2_full300_20260306_072643/pt_pc33mesh33udf33_reconch_g2_e300/ckpt_final.pt` + `data/shapenet_unpaired_cache_v2_pc33_mesh33_udf33` | `results/local_patchnepa_cpac/l001_g2_pc33_cmp.json` | Compare against `g0 composite` mini-CPAC (`iou@0.01=0.0948`, `rmse=0.09929`). If either metric improves, mark `g2 CPAC favorable`; if both worsen, mark `FT-only gain`. | `restart_plan -> runlog -> storyline if favorable` | Same `PC context -> UDF query`, `64/64` mini-CPAC protocol as the canonical g0 rerun. |
| `L002` | `20` | For CPAC, is `mesh50udf50` or `pc33mesh33udf33` the better `g2` source ckpt under the same eval protocol? | `B` | `queued` | `<=2h` | none | `runs/patchnepa_tokens/patchnepa_recong2_full300_20260306_072643/pt_mesh50udf50_reconch_g2_e300/ckpt_final.pt` + `data/shapenet_unpaired_cache_v2_pc33_mesh33_udf33` | `results/local_patchnepa_cpac/l002_g2_mesh50udf50_cmp.json` | Use the same `iou@0.01` and `rmse` criteria as `L001`; then rank `mesh50udf50` vs `pc33mesh33udf33` under one fixed CPAC protocol. | `restart_plan -> runlog -> storyline if source ranking changes` | Eval root stays fixed to `pc33_mesh33_udf33` so only the pretrain source changes. |
| `L003` | `30` | Can strict `pointgpt_ctx_only` run cleanly enough to give a real short-readout? | `B` | `queued` | `<=2h` | none | `nepa3d/configs/shapenet_unpaired_mix_v2_tokens_drop1_pc100.yaml` | `runs/local_patchnepa/l003_pgptctxonly_pc100_s2000/ckpt_final.pt` | If the run finishes without graph/DDP failure and yields `recon_lift_q >= 0`, promote to a longer `10k` parity test; otherwise mark the parity branch `blocked`. | `restart_plan -> runlog` | Single-GPU local rerun is intentional because the earlier full run failed before scientific readout. |
| `L004` | `40` | Under `g2`, does `answer_only` keep a CPAC advantage without destroying reconstruction-side lift? | `C` | `queued` | `<=12h` | none | `nepa3d/configs/shapenet_unpaired_mix_v2_tokens_drop1_pc33_mesh33_udf33.yaml` | `runs/local_patchnepa/l004_g2_{cmp,ans}_pc33_s2000` + `results/local_patchnepa_cpac/l004_g2_{cmp,ans}_pc33.json` | `answer_only` only graduates if it keeps `recon_lift_q >= 0` and beats composite on mini-CPAC by `iou@0.01 >= composite + 0.005`. | `restart_plan -> runlog -> storyline if promoted` | Queue manifest splits this into two runnable arms: `composite` and `answer_only`. |
| `L005` | `50` | Does true `fps_then_sample` parity matter once `point_all > npoints` is real rather than a permutation-only proxy? | `B` | `blocked` | `<=2h` | real `point_all > npoints` input path required | current `scanobjectnn_*_v3_nonorm` cache is only `2048` points | target path not yet staged | Require at least two seeds with the same sign of delta before taking the effect seriously; single-run parity is not evidence. | `restart_plan -> runlog if unstaged input path is solved` | Do not rerun this on the current `2048`-point cache. |
| `L006` | `60` | Does adding rotation at FT materially improve `g2 obj_only` credibility? | `C` | `deferred` | `<=12h` | finish `L001-L004` first | `runs/patchnepa_tokens/patchnepa_recong2_full300_20260306_072643/pt_pc33mesh33udf33_reconch_g2_e300/ckpt_final.pt` + strict `obj_only` FT recipe | target run dir under `runs/local_patchnepa_ft/` | Promote only if `obj_only` gains `>= +1.0pt`; smaller movement stays a secondary recipe note. | `benchmark + restart_plan + runlog if executed` | Use direct `finetune_patch_cls.py` CLI if custom rotation knobs are needed; the shared wrapper only exposes `aug_preset`. |
| `L007` | `70` | Can the current failure mode be formalized into a one-page paper-ready diagnostic pack? | `A` | `manual` | `<=2h` | none | canonical cosine/recon metrics already stored in docs + W&B summaries | `docs/patch_nepa` figure/table spec and metric sheet | Done when `cos_tgt`, `cos_prev`, `gap`, `copy_win`, `recon_lift_q/a`, and `target_std_mean/min` are unified into one stable page. | `storyline + paper notes` | This is analysis work, not a queue-first GPU run. |
| `L008` | `80` | Is any new cosine-family escape attempt still worth spending local budget on? | `A` | `deferred` | `<=2h` | only reopen if a genuinely new target family appears | current docs already fix the decision boundary: minor cosine-only tweaks are stop | none | Keep out of the main queue unless a new objective family changes the causal picture. | `hypothesis_matrix only if reopened` | This row exists to preserve the negative-result boundary. |

## 6. Queue Mapping

- machine-readable queue manifest:
  - `scripts/local/patchnepa_local_queue.tsv`
- maintained local runner:
  - `scripts/local/patchnepa_local_queue_runner.sh`
- status view:
  - `scripts/local/patchnepa_local_status.sh`

Queue policy:

- `enabled=1` rows are runnable now
- `enabled=0` rows remain visible but are not launched by the runner
- current enabled set is intentionally limited to the visibility-first branch:
  - `L000A`
- CQA rows `C001-C004` are maintained here as canonical gates, but are executed
  via the ABCI wrappers rather than the local queue runner
- `L001-L004` stay queued but gated behind `L000A/L000B`
